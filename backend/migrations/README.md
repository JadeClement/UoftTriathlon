## Database migrations

This folder contains all SQL migration files for the backend.

- Files are named `migrate-*.sql` with optional `-apply` / `-rollback` suffixes.
- Use the `backend/utils/migration-runner.js` script or `psql` directly to preview/apply them.

